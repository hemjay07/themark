# design/surfaces.yaml -- one entry per route. headline: the one line the surface exists to say;
# it must be the largest text once allow_larger is set aside. Generated by init.mjs from the html files; fill the headlines.
surfaces:

